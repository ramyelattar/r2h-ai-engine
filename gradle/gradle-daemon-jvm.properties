#This file is generated by updateDaemonJvm
# Daemon JVM criteria: any Java SE 21 toolchain (any vendor).
# The previous 'toolchainVendor=jetbrains' pin had no technical justification:
# nothing in the build requires JBR-specific capabilities, and the vendor pin
# could not be auto-provisioned by the foojay resolver on Windows.
# Gradle resolves this via JAVA_HOME / local installations, or auto-downloads
# a suitable JDK 21 through the foojay resolver configured in settings.gradle.kts.
toolchainVersion=21
