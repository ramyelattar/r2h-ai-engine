// NOTE: This Java implementation was retained as a transitional backup in the past.
// The canonical declaration for `ConnectedAppInfo` is now the Kotlin @Parcelize
// data class at `ConnectedAppInfo.kt`. Keeping two classes with the same
// fully-qualified name causes Kotlin compiler redeclaration errors. To avoid
// breaking existing callers while preserving a backup, this file has been
// intentionally emptied. If you need the old Java implementation, restore it
// from version control history instead of keeping both declarations live.

// Backup placeholder: no class is declared here intentionally.


